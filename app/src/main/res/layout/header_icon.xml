<?xml version="1.0" encoding="utf-8"?>
<merge
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:transitionName="container_transition"
    >

    <LinearLayout
        android:id="@+id/container"
        android:transitionName="header_transition"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scaleType="centerCrop"
        android:orientation="vertical"
        android:padding="@dimen/rc_item_padding">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <ImageView
                android:id="@+id/imageView_header_icon"
                android:layout_width="@dimen/rc_icon_size_l"
                android:layout_height="@dimen/rc_icon_size_l"
                android:layout_gravity="center"
                android:src="@drawable/ic_launcher_background">
            </ImageView>

            <LinearLayout
                android:layout_width="0dp"
                android:layout_weight="0.4"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/textView_header_title"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textSize="@dimen/extra_large_text_size"
                    android:padding="@dimen/rc_text_padding"
                    android:text="Title" />

                <TextView
                    android:id="@+id/textView_header_subtitle"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textSize="@dimen/large_text_size"
                    android:padding="@dimen/rc_text_padding"
                    android:text="Subtitle"
                    android:visibility="gone"/>


            </LinearLayout>

            <Button
                android:id="@+id/button_header_icon"
                android:layout_gravity="center"
                android:layout_width="@dimen/rc_icon_size_l"
                android:layout_height="@dimen/rc_icon_size_l"
                android:visibility="gone"/>

        </LinearLayout>

        <include
            layout="@layout/vertical_divider"/>

    </LinearLayout>

</merge>